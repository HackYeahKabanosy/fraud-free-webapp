# Fraud Free Webapp


### run

1. Install dependencies
`npm i`

2. Copy env files (you need to have a Mongodb running)
`cp .env.example .env` 

3. Run
`npm run start:dev`

4. Access the localhost
http://localhost:3001

